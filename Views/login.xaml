<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Reporteasyy.Views.login"
             Title="Log In">
   
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Entry x:Name="Username" FontFamily="Roboto" Placeholder="Email" BackgroundColor="#F8F8F8" HeightRequest="40" VerticalOptions="CenterAndExpand" Grid.Row="0"/>
        <Entry x:Name="Password" FontFamily="Roboto" Placeholder="Password" IsPassword="True" BackgroundColor="#F8F8F8" HeightRequest="40" VerticalOptions="CenterAndExpand" Margin="0,10,0,0" Grid.Row="1"/>

        <Button Text="Login" Clicked="OnSignInClicked" HorizontalOptions="FillAndExpand" VerticalOptions="CenterAndExpand" TextColor="White" FontAttributes="Bold" BackgroundColor="#FFD700" Margin="0,20,0,0" Grid.Row="2"/>
    </Grid>

</ContentPage>